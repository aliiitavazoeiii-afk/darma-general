services:
  expense-web:
    build:
      context: .
      dockerfile: Dockerfile.expense
    restart: unless-stopped
    env_file:
      - /opt/darma-general/.env
    environment:
      DJANGO_SETTINGS_MODULE: expense_site.settings
      DB_HOST: db
      EXPENSE_ALLOWED_HOSTS: kharj.filmjadiid.ir
      EXPENSE_CSRF_TRUSTED_ORIGINS: https://kharj.filmjadiid.ir
      EXPENSE_USE_HTTPS: "1"
    entrypoint: ["/bin/sh", "/app/expense_entrypoint.sh"]
    ports:
      - "${EXPENSE_BIND:-0.0.0.0}:${EXPENSE_PORT:-8011}:8000"
    networks:
      - darma_main

networks:
  darma_main:
    external: true
    name: "${DARMA_NETWORK}"
